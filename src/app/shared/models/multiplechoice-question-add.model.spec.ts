import { MultiplechoiceQuestionAdd } from './multiplechoice-question-add.model';

describe('MultiplechoiceQuestionAdd', () => {
  it('should create an instance', () => {
    expect(new MultiplechoiceQuestionAdd()).toBeTruthy();
  });
});
