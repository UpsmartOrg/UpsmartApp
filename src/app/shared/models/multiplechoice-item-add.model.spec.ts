import { MultiplechoiceItemAdd } from './multiplechoice-item-add.model';

describe('MultiplechoiceItemAdd', () => {
  it('should create an instance', () => {
    expect(new MultiplechoiceItemAdd()).toBeTruthy();
  });
});
