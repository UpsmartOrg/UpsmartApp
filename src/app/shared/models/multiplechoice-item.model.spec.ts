import { MultiplechoiceItem } from './multiplechoice-item.model';

describe('MultiplechoiceItem', () => {
  it('should create an instance', () => {
    expect(new MultiplechoiceItem()).toBeTruthy();
  });
});
